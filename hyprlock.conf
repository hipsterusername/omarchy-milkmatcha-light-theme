# Milkmatcha Theme - Creamy Tea Garden
# Warm, cozy lock screen with matcha green accents

# Required variables for main hyprlock.conf
$color = rgba(244,241,232,0.95)       # Creamy milk tea background
$inner_color = rgba(224,217,200,0.95) # Light foam inner
$outer_color = rgba(122,148,97,1.0)   # Fresh matcha border
$font_color = rgba(92,106,83,1.0)     # Deep matcha text
$check_color = rgba(143,172,112,1.0)  # Bright matcha success

# Additional theme colors
$text_accent = rgba(122,148,97,1.0)   # Fresh matcha accent
$error = rgba(198,95,95,1.0)          # Soft red bean error
$golden = rgba(212,165,116,1.0)       # Golden milk tea

# Matcha Steam Cup - Line 1 (Steam)
label {
    monitor =
    text =         ∿∿∿
    color = rgba(92,106,83,1.0)
    font_size = 16
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 400
    halign = center
    valign = center
}

# Matcha Steam Cup - Line 2 (Steam)
label {
    monitor =
    text =        ∿∿∿∿∿
    color = rgba(92,106,83,1.0)
    font_size = 16
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 380
    halign = center
    valign = center
}

# Matcha Steam Cup - Line 3 (Steam)
label {
    monitor =
    text =       ∿∿∿∿∿∿∿
    color = rgba(92,106,83,1.0)
    font_size = 16
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 360
    halign = center
    valign = center
}

# Matcha Steam Cup - Line 4 (Cup top)
label {
    monitor =
    text =      ╔═══════╗
    color = rgba(92,106,83,1.0)
    font_size = 16
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 340
    halign = center
    valign = center
}

# Matcha Steam Cup - Line 5 (Cup body)
label {
    monitor =
    text =      ║ ░░░░░ ║
    color = rgba(92,106,83,1.0)
    font_size = 16
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 320
    halign = center
    valign = center
}

# Matcha Steam Cup - Line 6 (Cup body)
label {
    monitor =
    text =      ║ ▒▒▒▒▒ ║
    color = rgba(92,106,83,1.0)
    font_size = 16
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 300
    halign = center
    valign = center
}

# Matcha Steam Cup - Line 7 (Cup body)
label {
    monitor =
    text =      ║ ▓▓▓▓▓ ║
    color = rgba(92,106,83,1.0)
    font_size = 16
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 280
    halign = center
    valign = center
}

# Matcha Steam Cup - Line 8 (Cup bottom)
label {
    monitor =
    text =      ╚═══════╝
    color = rgba(92,106,83,1.0)
    font_size = 16
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 260
    halign = center
    valign = center
}

# Matcha Steam Cup - Line 9 (Base)
label {
    monitor =
    text =       ╰─────╯
    color = rgba(92,106,83,1.0)
    font_size = 16
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 240
    halign = center
    valign = center
}

# Time display - matcha green, positioned higher
label {
    monitor =
    text = $TIME
    color = $text_accent
    font_size = 64
    font_family = JetBrainsMono Nerd Font Light
    position = 0, 200
    halign = center
    valign = center
}

# Date display - deep matcha, positioned higher
label {
    monitor =
    text = cmd[update:1000] echo "$(date +'%A, %B %d')"
    color = rgba(92,106,83,0.8)
    font_size = 16
    font_family = JetBrainsMono Nerd Font
    position = 0, 140
    halign = center
    valign = center
}